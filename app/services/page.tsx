import type { Metadata } from "next";
import Link from "next/link";
import { site } from "@/content/site";
import FadeIn from "@/components/FadeIn";
export const metadata:Metadata={title:"What we handle — Random Creation",description:site.services.intro};
export default function Services(){return <><section className="inner-hero"><div className="shell"><FadeIn><p className="eyebrow">What we handle</p><h1>{site.services.title}</h1><p>{site.services.intro}</p></FadeIn></div></section><section className="page-section"><div className="shell"><div className="service-grid">{site.three.map((s,i)=><FadeIn key={s.num} delay={i*80}><article className="service-card"><div className="service-number">{s.num}</div><span className="service-tag">{s.tag}</span><h3>{s.title}</h3><p>{s.description}</p><ul className="service-list">{s.includes.map(x=><li key={x}><span className="check">✓</span>{x}</li>)}</ul><div className="service-outcome"><span className="check">✓</span>{s.outcome}</div></article></FadeIn>)}</div></div></section><section className="closing"><div className="shell"><FadeIn><p className="eyebrow">A useful first step</p><h2>See where your customer experience is leaking revenue.</h2><p>Twenty minutes. Real findings. No sales pitch.</p><div className="button-row center-row"><Link href="/book" className="btn-light">Book my free check-up →</Link></div></FadeIn></div></section></>}
